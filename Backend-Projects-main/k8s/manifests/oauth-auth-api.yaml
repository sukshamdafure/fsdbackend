apiVersion: apps/v1
kind: Deployment
metadata:
  name: oauth-auth-api
  namespace: backend-projects
  labels:
    app: oauth-auth-api
    category: auth
spec:
  replicas: 2
  selector:
    matchLabels:
      app: oauth-auth-api
  template:
    metadata:
      labels:
        app: oauth-auth-api
        category: auth
    spec:
      containers:
      - name: oauth-auth-api
        image: oauth-auth-api:latest
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: NODE_ENV
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: PORT
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: oauth-auth-api-service
  namespace: backend-projects
  labels:
    app: oauth-auth-api
spec:
  selector:
    app: oauth-auth-api
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP